"""Optional providers; import an adapter from its module after installing its extra.

Keeping this module free of provider imports allows the core package to work without
HTTP or downloader dependencies.
"""
